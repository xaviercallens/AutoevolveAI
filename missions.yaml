# missions.yaml — PR Factory Mission Manifest
# Each mission is dispatched to Jules as an autonomous engineering task.
# Types: performance, qa_fuzzing, security, lean_proof, custom

missions:
  - type: performance
    target: anse/symbolic/sandbox.py
    description: "Optimize sandbox executor hot path — reduce subprocess overhead"

  - type: qa_fuzzing
    target: anse/jepa
    description: "Add property-based tests for JEPA trainer dimension matching"

  - type: security
    target: gateway.py
    description: "Audit request injection vectors in the intercept gateway"

  - type: lean_proof
    target: formal/ANSE/Autopoiesis.lean
    description: "Extend autopoiesis_exists to cover the Banach fixed-point contraction mapping"

  - type: performance
    target: anse/micro_ml/modules.py
    description: "Optimize MicroCodePredictor forward pass under 50k parameter budget"
