# Quarantine

This directory holds modules that reported success without doing the work. No module in this directory may be imported by `anse/`, `scripts/`, or `tests/` except by a test that explicitly asserts the module is NOT imported.

## Entries

<!-- Entries list: {original_path} | {audit_finding} | {date} -->

