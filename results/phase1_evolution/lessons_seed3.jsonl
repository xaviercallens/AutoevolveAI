{"task": "Write a function `rotate_list(lst: list, k: int) -> list` that returns a new list rotated to the right by k positions. k may be larger than the list length or zero, and the list may be empty.", "code": "def rotate_list(lst: list, k: int) -> list:\n    if not lst:\n        return []\n\n    # Normalize k to avoid unnecessary rotations\n    k %= len(lst)\n\n    # Perform the rotation\n    rotated = lst[-k:] + lst[:-k]\n\n    return rotated", "failure": "", "iterations": 1, "timestamp": 1790209279.8046577}
